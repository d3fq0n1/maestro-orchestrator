version: "3.9"
services:
  maestro:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - maestro-sessions:/app/data/sessions
      - maestro-r2:/app/data/r2

volumes:
  maestro-sessions:
  maestro-r2:
